describe('Javascript é assim', () => {
    test('Igualdade?', () => {
        expect(false == 0).toBe(true)
        expect('' == false).toBe(true)
        expect(null == undefined).toBe(true)
    })
})